<div class="d-flex justify-content-center align-items-center vh-100 bg-dark text-white">
  <div class="card shadow-lg p-4" style="width: 400px; border-radius: 15px;">
    <h4 class="text-center mb-4 text-dark">🔑 Redefinir Senha</h4>

    <%= form_with model: resource, url: password_path(resource_name), local: true do |f| %>
      <div class="mb-3">
        <%= f.label :email, "E-mail", class: "form-label text-dark" %>
        <%= f.email_field :email, autofocus: true, class: "form-control" %>
      </div>

      <%= f.submit "Enviar link de redefinição", class: "btn btn-primary w-100" %>
    <% end %>

    <div class="text-center mt-3">
      <%= link_to "Voltar ao login", new_session_path(resource_name), class: "text-decoration-none small text-primary" %>
    </div>
  </div>
</div>
