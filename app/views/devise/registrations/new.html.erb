<div class="d-flex justify-content-center align-items-center vh-100 bg-light">
  <div class="card shadow-lg p-4" style="width: 420px; border-radius: 15px;">
    <h3 class="text-center mb-4">🧾 Cadastro de Usuário</h3>

    <%= form_with model: resource, url: registration_path(resource_name), local: true do |f| %>
      <div class="mb-3">
        <%= f.label :email, "E-mail", class: "form-label" %>
        <%= f.email_field :email, autofocus: true, class: "form-control", placeholder: "seu@email.com" %>
      </div>

      <div class="mb-3">
        <%= f.label :password, "Senha", class: "form-label" %>
        <%= f.password_field :password, class: "form-control", placeholder: "mínimo 6 caracteres" %>
      </div>

      <div class="mb-3">
        <%= f.label :password_confirmation, "Confirmar senha", class: "form-label" %>
        <%= f.password_field :password_confirmation, class: "form-control" %>
      </div>

      <%= f.submit "Cadastrar", class: "btn btn-success w-100" %>
    <% end %>

    <hr>
    <div class="text-center">
      <%= link_to "Já tem conta? Entrar", new_session_path(resource_name), class: "btn btn-outline-primary btn-sm w-100" %>
    </div>
  </div>
</div>
