<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <title>NF Felipet - Home</title>
    <link rel="icon" type="image/png" href="https://cdn-icons-png.flaticon.com/512/1603/1603575.png">

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

<style>
  /* Fundo */
  body.landing-page {
    background: url('/eee.png') no-repeat bottom center fixed;
    background-size: 100%; /* 🔥 um pouco menos de zoom */
    position: relative;
  }

  /* Overlay */
  .overlay {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.55);
    z-index: -1;
  }

  /* Container central */
  .landing-container {
    height: 40vh;
  }

  /* Box */
  .welcome-box {
    max-width: 650px;
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(8px);
    padding: 40px;
    border-radius: 16px;
    box-shadow: 0 0 20px rgba(0,0,0,0.3);
  }

  .welcome-title {
    font-size: 2.4rem;
    font-weight: 700;
  }

  /* 📱 Mobile */
  @media (max-width: 768px) {

    /* Fundo menos zoom no celular e sempre colado embaixo */
    body.landing-page {
      background-size: 200%;
      background-position: bottom center;
    }

    /* Altura maior no celular */
    .landing-container {
      height: 60vh;
    }

    .welcome-title {
      font-size: 1.9rem;
    }
  }
</style>

  </head>

  <body class="landing-page text-white">

    <div class="overlay"></div>

    <!-- Navbar -->
   <nav class="navbar navbar-expand-lg navbar-dark" style="background:#0d1b2a;">

      <div class="container">

        <a class="navbar-brand d-flex align-items-center" href="/">
          <i class="fa-solid fa-file-invoice-dollar text-warning me-2"></i>
          <strong>NF Felipet</strong>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarHome">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarHome">
          <ul class="navbar-nav ms-auto">

            <li class="nav-item">
              <%= link_to new_user_session_path,
                    class: "btn btn-outline-light px-4" do %>
                <i class="fa-solid fa-right-to-bracket me-2"></i>
                Entrar
              <% end %>
            </li>

          </ul>
        </div>
      </div>
    </nav>

    <!-- Conteúdo Central -->
    <div class="d-flex justify-content-center align-items-center landing-container">
      <div class="welcome-box text-center">

        <h1 class="welcome-title mb-3">
          Bem-vindo ao Meu Sistema de Emissão de NFe
        </h1>

        <p class="welcome-sub mb-4">
          Um sistema moderno, feito para portifolio, rápido e seguro para emissão, consulta e gerenciamento de Notas Fiscais Eletrônicas.
        </p>

        <%= link_to new_user_session_path,
              class: "btn btn-lg btn-outline-warning px-5 py-2" do %>
          <i class="fa-solid fa-arrow-right-to-bracket me-2"></i>
          Acessar Sistema
        <% end %>

      </div>
    </div>

  </body>
</html>
