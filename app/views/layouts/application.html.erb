<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <title>NF Emissor</title>
    <link rel="icon" type="image/png" href="https://cdn-icons-png.flaticon.com/512/1603/1603575.png">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- ✅ Bootstrap CSS (sem integrity) -->
<link
href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
rel="stylesheet"
crossorigin="anonymous">

<!-- ✅ Bootstrap Icons -->
<link
rel="stylesheet"
href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css"
crossorigin="anonymous">

<!-- ✅ Font Awesome 6.5.1 (versão confiável e liberada) -->
<link
rel="stylesheet"
href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
crossorigin="anonymous">


    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- ✅ Bootstrap JS -->
   <!-- ✅ Bootstrap JS (sem integrity) -->
<script
src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
crossorigin="anonymous"></script>


    <style>
 /* Sidebar padrão */
.sidebar {
  width: 240px;
  min-height: 100vh;
  transition: width 0.25s ease;
}

/* Links */
.sidebar-link {
  padding: 10px 14px;
  border-radius: 6px;
  transition: background 0.2s ease;
}

.sidebar-link:hover {
  background: rgba(255,255,255,0.1);
}

/* Ícones */
.sidebar-link i {
  font-size: 1.2rem;
}

/* ----------------------------- */
/* TABLET — sidebar estreita     */
/* ----------------------------- */
@media (max-width: 992px) {
  .sidebar {
    width: 80px; /* igual ao modelo da imagem */
  }

  /* Esconde textos */
  .sidebar span {
    display: none;
  }

  /* Esconde título normal */
  .logo-text {
    display: none;
  }

  /* Centraliza o ícone do topo */
  .sidebar-header {
    justify-content: center;
  }

  /* Ícones centralizados */
  .sidebar-link {
    justify-content: center;
  }

  .sidebar-link i {
    margin: 0;
  }
}

    </style>
  </head>

  <body class="bg-light">
    <% if user_signed_in? %>
      <div class="d-flex">
        <!-- 🧭 Sidebar -->
        <nav class="sidebar bg-dark text-white p-3">

          <!-- Cabeçalho -->
          <div class="sidebar-header d-flex align-items-center mb-4">
            <i class="fa-solid fa-file-invoice-dollar text-warning fs-4 me-2 logo-icon"></i>
            <span class="logo-text h5 m-0">NF Emissor</span>
          </div>
        
          <ul class="nav flex-column">
        
            <li class="nav-item">
              <%= link_to authenticated_root_path, class: "nav-link d-flex align-items-center sidebar-link" do %>
                <i class="fa-solid fa-chart-line me-2"></i>
                <span>Dashboard</span>
              <% end %>
            </li>
        
            <li class="nav-item">
              <% if current_user.certificate_installed? %>
                <%= link_to new_notas_fiscai_path, class: "nav-link d-flex align-items-center sidebar-link" do %>
                  <i class="fa-solid fa-file-circle-plus me-2"></i>
                  <span>Emitir Nota</span>
                <% end %>
              <% else %>
                <a class="nav-link disabled d-flex align-items-center sidebar-link">
                  <i class="fa-solid fa-file-circle-plus me-2"></i>
                  <span>Emitir Nota</span>
                </a>
              <% end %>
            </li>
        
            <li class="nav-item">
              <% if current_user.certificate_installed? %>
                <%= link_to notas_fiscais_path, class: "nav-link d-flex align-items-center sidebar-link" do %>
                  <i class="fa-solid fa-folder-open me-2"></i>
                  <span>Notas Emitidas</span>
                <% end %>
              <% else %>
                <a class="nav-link disabled d-flex align-items-center sidebar-link">
                  <i class="fa-solid fa-folder-open me-2"></i>
                  <span>Notas Emitidas</span>
                </a>
              <% end %>
            </li>
        
            <li class="nav-item">
              <%= link_to new_certificate_path, class: "nav-link d-flex align-items-center sidebar-link" do %>
                <i class="fa-solid fa-shield-halved me-2"></i>
                <span>Certificado</span>
              <% end %>
            </li>
        
            <li class="nav-item mt-4">
              <%= link_to destroy_user_session_path,
                          data: { turbo_method: :delete },
                          class: "nav-link text-danger d-flex align-items-center sidebar-link" do %>
                <i class="fa-solid fa-right-from-bracket me-2"></i>
                <span>Sair</span>
              <% end %>
            </li>
        
          </ul>
        
        </nav>
        
        

        <!-- 📊 Conteúdo principal -->
        <main class="flex-grow-1 p-4">
          <% if notice %>
            <div class="alert alert-success"><%= notice %></div>
          <% end %>
          <% if alert %>
            <div class="alert alert-danger"><%= alert %></div>
          <% end %>

          <% if user_signed_in? && !current_user.certificate_installed? %>
            <div class="alert alert-warning d-flex justify-content-between align-items-center">
              <div>
                <strong>Certificado obrigatório!</strong>
                Configure o certificado digital para emitir e visualizar notas.
              </div>
              <%= link_to "Configurar", new_certificate_path, class: "btn btn-sm btn-primary" %>
            </div>
          <% end %>

          <%= yield %>
        </main>
      </div>
    <% else %>
      <div class="container mt-5">
        <%= yield %>
      </div>
    <% end %>
  </body>
</html>
